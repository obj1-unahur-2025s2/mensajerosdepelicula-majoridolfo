import mensajeros.*
import vehiculos.*
import destinosYPaquetes.*
import empresa.*


describe " test ejercicio"{
test " roberto puede llevar un paquete pago en un camion con 1 acoplado"{
   roberto.cambiarVehiculo(camion)
   camion.cantidadAcoplados(1)
   paquete.estaPago()
   assert.that(paquete.puedeSerEntregado(roberto))
}
}
test "neo no puedellevar paquete no pago a matrix"{
  paquete.estaPago()
  assert.notThat(paquete.puedeSerEntregado(neo))
}